package Aptitude;

import java.io.IOException;


// NOT COMPLETED
public class DublicateLettersTesting {
	/*
	 * static long repeatedString(String s, long n) {
	 * 
	 * long size = s.length(), repeated = n / size; long left = n - (size *
	 * repeated); int extra = 0;
	 * 
	 * int count = 0; for (int i = 0; i < size; i++) { if (s.charAt(i) == 'a') {
	 * ++count; } }
	 * 
	 * for (int i = 0; i < left; i++) { if (s.charAt(i) == 'a') { ++extra; } }
	 * 
	 * repeated = (repeated * count) + extra; System.out.println("repeated -->" +
	 * repeated); return repeated; }
	 * 
	 * 
	 * 
	 * 
	 * public static void main(String[] args) throws IOException {
	 * 
	 * String s = "aba"; int n = 10;
	 * 
	 * long result = repeatedString(s, n); }
	 */
}
